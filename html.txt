<!DOCTYPE html>
<html>
<head>
  <title>100 Day Challenge</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #1e1e2f, #2c2c54);
      color: white;
      text-align: center;
      padding: 20px;
    }

    h1 {
      font-size: 28px;
    }

    input, button {
      padding: 10px;
      margin: 10px;
      border-radius: 8px;
      border: none;
    }

    button {
      background: #ff6b6b;
      color: white;
      cursor: pointer;
    }

    button:hover {
      background: #ff4757;
    }

    .days {
      display: grid;
      grid-template-columns: repeat(10, 1fr);
      gap: 5px;
      margin-top: 20px;
    }

    .day {
      padding: 10px;
      background: #57606f;
      border-radius: 6px;
      cursor: pointer;
    }

    .completed {
      background: #2ed573;
    }

    .ai-box {
      margin-top: 20px;
      padding: 15px;
      background: #3742fa;
      border-radius: 10px;
    }
  </style>
</head>

<body>

  <h1>🔥 100 Day Challenge</h1>

  <input type="text" id="taskInput" placeholder="Enter today's task">
  <button onclick="saveTask()">Save Task</button>

  <div class="ai-box" id="aiMessage">
    Welcome! Start your journey today 🚀
  </div>

  <div class="days" id="daysContainer"></div>

<script>
  const daysContainer = document.getElementById("daysContainer");
  const aiMessage = document.getElementById("aiMessage");

  let completedDays = JSON.parse(localStorage.getItem("completedDays")) || [];
  let tasks = JSON.parse(localStorage.getItem("tasks")) || {};

  function generateDays() {
    daysContainer.innerHTML = "";
    for (let i = 1; i <= 100; i++) {
      const day = document.createElement("div");
      day.classList.add("day");
      day.innerText = i;

      if (completedDays.includes(i)) {
        day.classList.add("completed");
      }

      day.onclick = function () {
        toggleDay(i);
      };

      daysContainer.appendChild(day);
    }
  }

  function toggleDay(day) {
    if (!completedDays.includes(day)) {
      completedDays.push(day);
      showAIMessage("Amazing! Day " + day + " completed 💪 Keep going!");
    } else {
      completedDays = completedDays.filter(d => d !== day);
      showAIMessage("Day " + day + " removed. Stay focused 🔥");
    }

    localStorage.setItem("completedDays", JSON.stringify(completedDays));
    generateDays();
  }

  function saveTask() {
    const task = document.getElementById("taskInput").value;
    const today = new Date().getDate();

    tasks[today] = task;
    localStorage.setItem("tasks", JSON.stringify(tasks));

    showAIMessage("Task saved! You are building discipline 🚀");
    document.getElementById("taskInput").value = "";
  }

  function showAIMessage(message) {
    aiMessage.innerText = message;
  }

  generateDays();
</script>

</body>
</html>